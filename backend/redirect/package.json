{
  "name": "redirect",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV=production node dist/index.js",
    "build": "esbuild src/index.ts --bundle --platform=node --outfile=dist/index.js --format=esm  --external:node:* --external:path --packages=external && npx prisma generate",
    "dev:node": "NODE_ENV=development node --watch dist/index.js",
    "dev:tsc": "NODE_ENV=development nodemon --watch src --ext ts --exec ts-node src/index.ts",
    "check": "tsc --noEmit src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.11.1",
    "dotenv": "^17.2.3",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.3",
    "esbuild": "^0.27.2",
    "nodemon": "^3.1.11",
    "prisma": "^6.11.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
