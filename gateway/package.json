{
  "name": "url_shorter_gateway",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/index.js",
    "build": "esbuild src/gateway.ts --bundle --platform=node --outfile=dist/gateway.js --format=esm  --external:node:* --external:path --packages=external",
    "dev:node": "node --watch dist/gateway.js",
    "dev:tsc": "nodemon --watch src --ext ts --exec ts-node src/gateway.ts",
    "check": "tsc --noEmit"
  },
  "type": "module",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "http-proxy-middleware": "^3.0.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.15.17",
    "esbuild": "^0.25.4",
    "jest": "^29.7.0",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
